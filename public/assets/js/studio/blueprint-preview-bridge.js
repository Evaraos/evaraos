function toast(message){let node=document.querySelector('.builder-toast');if(!node){node=document.createElement('div');node.className='builder-toast';document.body.appendChild(node);}node.textContent=message;node.classList.add('is-visible');clearTimeout(toast.timer);toast.timer=setTimeout(()=>node.classList.remove('is-visible'),1500);}
function selectedBlueprint(){try{return JSON.parse(localStorage.getItem('evaraos-studio-home-draft-v1')||'{}').selectedBlueprintId||'owner';}catch{return 'owner';}}
function routeFor(role){if(role==='customer')return'/customer_dashboard.html';if(['technician','cleaner','sales','vendor'].includes(role))return'/dashboard.html';return'/dashboard.html';}
function bind(){document.addEventListener('click',(event)=>{const button=event.target.closest('[data-blueprint-action="preview"]');if(!button)return;event.preventDefault();event.stopPropagation();const role=selectedBlueprint();if(window.EvaraRolePreview?.set)window.EvaraRolePreview.set(role);else localStorage.setItem('evaraos-preview-role',role);toast(`Previewing ${role}`);setTimeout(()=>{const target=routeFor(role);if(location.pathname!==target)location.assign(target);else window.dispatchEvent(new CustomEvent('evara:role-preview',{detail:{role}}));},450);},true);}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',bind,{once:true});else bind();