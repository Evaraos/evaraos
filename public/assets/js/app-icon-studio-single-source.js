(() => {
  'use strict';
  const KEY = 'evaraos-app-icon-selection-v2';
  const CUSTOM_KEY = 'evaraos-custom-app-icon-v4';
  const ICON = '/assets/brand/evaraos-app-icon.png?v=brand-icon-4';
  const OPTIONS = [['none','Official Icon','Official Evaraos app icon'],['pearlWhite','Pearl White','Clean premium light surface'],['pianoBlack','Piano Black','Deep reflective black glass'],['crimsonGlass','Crimson Glass','Signature red Liquid Glass'],['graphite','Graphite','Executive graphite'],['titanium','Titanium','Brushed silver depth'],['midnightBlue','Midnight Blue','Dark navy finish'],['carbonFiber','Carbon Fiber','Technical woven carbon'],['rubyGlass','Ruby Glass','Polished ruby translucency'],['frostedClear','Frosted Clear','Soft translucent glass'],['satinSilver','Satin Silver','Muted premium metal'],['deepNavy','Deep Navy','Near-black blue depth'],['matteBlack','Matte Black','Minimal matte black'],['softIvory','Soft Ivory','Warm clean neutral'],['burgundy','Burgundy','Deep red luxury finish'],['liquidGlassWaves','Liquid Glass Waves','Slow refractive wave motion'],['auroraFlow','Aurora Flow','Adaptive color flow'],['orbitRings','Orbit Rings','Precision rings'],['neuralGrid','Neural Grid','Connected system grid'],['energyPulse','Energy Pulse','Focused energy pulse'],['redPlasma','Red Plasma','Fluid plasma field'],['lightSweepBg','Light Sweep','Clean highlight sweep'],['crystalRefraction','Crystal Refraction','Faceted light shifts'],['adaptiveGradient','Adaptive Gradient','Brand gradient'],['particleDrift','Particle Drift','Subtle particle depth'],['shineSweep','Shine Sweep','Specular highlight'],['hoverFloat','Hover Float','Gentle lift'],['magneticTilt','Magnetic Tilt','Dimensional tilt'],['breathingGlow','Breathing Glow','Soft red halo'],['orbitReflection','Orbit Reflection','Rotating reflections']];
  const byId = new Map(OPTIONS.map(([id,label,description]) => [id,{id,label,description}]));
  let active = byId.has(localStorage.getItem(KEY)) ? localStorage.getItem(KEY) : 'none';
  let customIcon = readCustomIcon();
  function readCustomIcon(){ try{ const saved = localStorage.getItem(CUSTOM_KEY); return saved && saved.startsWith('data:image/') ? saved : ''; }catch{return '';} }
  function installRepairStyle(){ let style = document.getElementById('appIconRuntimeRepair'); if(!style){ style = document.createElement('style'); style.id = 'appIconRuntimeRepair'; document.head.appendChild(style); } style.textContent = '.app-icon-preview,.app-icon-title-mark{position:relative!important}.app-icon-logo-img,.app-icon-preview.is-backgroundless .app-icon-logo-img,.app-icon-title-mark.is-backgroundless .app-icon-logo-img{display:block!important;opacity:1!important;visibility:visible!important;width:92%!important;height:92%!important;max-width:92%!important;max-height:92%!important;object-fit:contain!important;object-position:center!important;mix-blend-mode:normal!important;filter:none!important;z-index:6!important;position:relative!important}.app-icon-title-mark .app-icon-logo-img{width:88%!important;height:88%!important}.app-icon-current-preview .app-icon-logo-img{width:96%!important;height:96%!important}.app-icon-preview.is-backgroundless,.app-icon-title-mark.is-backgroundless{background:linear-gradient(145deg,rgba(255,255,255,.34),rgba(255,255,255,.12))!important}.app-icon-current-card,.app-icon-background-card,.app-icon-tips-card,.app-icon-device-card{background:linear-gradient(145deg,rgba(255,255,255,.30),rgba(255,255,255,.13))!important;border:1px solid rgba(255,255,255,.38)!important;backdrop-filter:blur(30px) saturate(1.35)!important;-webkit-backdrop-filter:blur(30px) saturate(1.35)!important}'; }
  function iconSrc(){ return customIcon || ICON; }
  function classFor(id, current){ return `${current ? 'app-icon-current-preview ' : ''}app-icon-preview app-icon-preview--${id} ${id === 'none' ? 'is-backgroundless' : ''}`; }
  function imgFor(){ const img = new Image(); img.className = 'app-icon-logo-img'; img.alt = ''; img.decoding = 'async'; img.loading = 'eager'; img.src = iconSrc(); img.onerror = () => { img.src = ICON; }; return img; }
  function paint(node,id,current=false){ if(!node) return; node.className = classFor(id,current); node.replaceChildren(imgFor()); }
  function paintTitle(node,id){ if(!node) return; node.className = `app-icon-title-mark ${id === 'none' ? 'is-backgroundless' : ''}`; node.replaceChildren(imgFor()); }
  function renderGrid(){ const grid = document.querySelector('[data-app-icon-grid]'); if(!grid) return; const frag = document.createDocumentFragment(); OPTIONS.forEach(([id,label,description], index) => { const button = document.createElement('button'); button.type = 'button'; button.className = `app-icon-choice${id === active ? ' is-active' : ''}`; button.dataset.appIconChoice = id; button.setAttribute('aria-pressed', String(id === active)); button.setAttribute('aria-label', `${label}. ${description}`); const preview = document.createElement('span'); preview.className = classFor(id,false); preview.replaceChildren(imgFor()); const copy = document.createElement('span'); copy.className = 'app-icon-copy'; copy.innerHTML = `<small>${String(index + 1).padStart(2,'0')}</small><strong>${label}</strong><em>${description}</em>`; button.append(preview, copy); frag.appendChild(button); }); grid.replaceChildren(frag); }
  function sync(){ installRepairStyle(); const option = byId.get(active) || byId.get('none'); paint(document.querySelector('[data-current-icon-preview]'), active, true); document.querySelectorAll('[data-device-preview],[data-device-preview-light]').forEach(node => paint(node, active)); document.querySelectorAll('[data-brand-title-mark]').forEach(node => paintTitle(node, active)); document.querySelectorAll('[data-current-icon-name]').forEach(node => { node.textContent = option.label; }); document.querySelectorAll('[data-app-icon-choice]').forEach(button => { const selected = button.dataset.appIconChoice === active; button.classList.toggle('is-active', selected); button.setAttribute('aria-pressed', String(selected)); }); }
  function setIcons(href){ ['icon','shortcut icon','apple-touch-icon'].forEach(rel => { let link = document.querySelector(`link[rel="${rel}"]`); if(!link){ link = document.createElement('link'); link.rel = rel; document.head.appendChild(link); } link.type = 'image/png'; link.href = href; }); }
  function toast(message){ let node = document.querySelector('.app-icon-toast'); if(!node){ node = document.createElement('div'); node.className = 'app-icon-toast'; document.body.appendChild(node); } node.innerHTML = `<strong>Saved</strong><span>${message}</span>`; node.classList.add('is-visible'); clearTimeout(toast.timer); toast.timer = setTimeout(() => node.classList.remove('is-visible'), 1500); }
  function save(){ try{ localStorage.setItem(KEY, active); }catch{} setIcons(iconSrc()); toast((byId.get(active)||byId.get('none')).label); }
  function select(id){ if(!byId.has(id)) return; active = id; sync(); save(); }
  function restore(){ active = 'none'; customIcon = ''; try{ localStorage.removeItem(CUSTOM_KEY); localStorage.setItem(KEY,'none'); }catch{} renderGrid(); sync(); save(); toast('Official icon restored'); }
  function upload(file){ if(!file){ return; } if(!/^image\/(png|jpeg|webp)$/.test(file.type)){ toast('Use PNG, JPG, or WEBP'); return; } const reader = new FileReader(); reader.onload = () => { customIcon = String(reader.result || ''); if(!customIcon.startsWith('data:image/')){ customIcon = ''; toast('Upload failed'); return; } try{ localStorage.setItem(CUSTOM_KEY, customIcon); }catch{} renderGrid(); sync(); save(); toast('Custom icon uploaded'); }; reader.onerror = () => toast('Upload failed'); reader.readAsDataURL(file); }
  function boot(){ installRepairStyle(); renderGrid(); sync(); setIcons(iconSrc()); document.querySelector('[data-app-icon-grid]')?.addEventListener('click', e => { const btn = e.target.closest('[data-app-icon-choice]'); if(btn) select(btn.dataset.appIconChoice); }); document.querySelectorAll('[data-apply-selected-icon]').forEach(btn => btn.addEventListener('click', save)); document.querySelectorAll('[data-restore-original-icon]').forEach(btn => btn.addEventListener('click', restore)); document.querySelectorAll('[data-custom-icon-upload]').forEach(input => input.addEventListener('change', () => upload(input.files?.[0]))); setTimeout(sync,500); setTimeout(sync,1500); }
  window.EvaraosAppIcons = { selectOption: select, restoreOriginal: restore, applySelection: save, forcePreviewSync: sync, uploadIcon: upload };
  document.readyState === 'loading' ? document.addEventListener('DOMContentLoaded', boot, {once:true}) : boot();
})();
