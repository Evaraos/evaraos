const root=document.documentElement;const body=document.body;let timer=0;function keyboardIsOpen(){const viewport=window.visualViewport;if(!viewport)return false;const layout=Math.max(window.innerHeight,root.clientHeight||0);return layout-Math.round(viewport.height)>120}function clearViewportState(){root.style.removeProperty('--messages-viewport-top');root.style.removeProperty('--messages-viewport-height');body.classList.remove('keyboard-open');if(!keyboardIsOpen())window.scrollTo({top:0,left:0,behavior:'instant'})}function stabilize(){const interactive=body.classList.contains('messages-chat-active')||body.classList.contains('messages-search-active');if(!keyboardIsOpen()||!interactive)clearViewportState()}function schedule(){clearTimeout(timer);timer=setTimeout(stabilize,0)}function lockInitialFrame(){root.classList.add('messages-viewport-locked');schedule();requestAnimationFrame(()=>{schedule();requestAnimationFrame(()=>root.classList.remove('messages-viewport-locked'))});setTimeout(schedule,120);setTimeout(schedule,420)}function cleanup(){clearTimeout(timer);clearViewportState();body.classList.remove('messages-search-active','messages-chat-active');root.classList.remove('messages-viewport-locked');delete root.dataset.activeConversation}if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',lockInitialFrame,{once:true});else lockInitialFrame();window.addEventListener('pageshow',lockInitialFrame);window.addEventListener('focus',schedule);window.addEventListener('pagehide',cleanup);window.addEventListener('beforeunload',cleanup);window.visualViewport?.addEventListener('resize',schedule,{passive:true});